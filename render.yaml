services:
  - type: web
    name: bestthing
    runtime: node
    plan: free
    region: oregon
    branch: main
    buildCommand: npm install && npx playwright install chromium
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
